server:
  port: 7070

spring:
  application:
    name: auth-api
  jpa:
    database: POSTGRESQL
    hibernate:
      ddl-auto: none
    show-sql: false
  datasource:
    url: jdbc:postgresql://localhost:5432/auth
    username: postgres
    password: coti
    driver-class-name: org.postgresql.Driver
  output:
    ansi:
      enabled: ALWAYS
